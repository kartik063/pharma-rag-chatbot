"""
Gradio UI package for pharma RAG.

Launches an interactive chat interface that calls PharmaAgent
under the hood, streaming responses from the LangGraph ReAct
agent loop to the browser.

Entry point:
    python -m ui.app
"""
