# Copy this file to .env and fill in your values.
# load_dotenv() in each module reads .env automatically on startup.

# Ollama model name — must be pulled first: ollama pull llama3.2
OLLAMA_MODEL=llama3.2

# Base URL of the running Ollama daemon (default port is 11434)
OLLAMA_BASE_URL=
